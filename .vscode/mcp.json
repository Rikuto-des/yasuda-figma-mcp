{
  "servers": {
    "yasuda-figma-mcp": {
      "type": "stdio",
      "command": "node",
      "args": ["--env-file-if-exists=${workspaceFolder}/.env", "${workspaceFolder}/dist/mcp.js"],
      "env": {
        "BRIDGE_URL": "ws://127.0.0.1:3055",
        "BRIDGE_CHANNEL": "default"
      }
    }
  }
}
